#ifndef __CMA_CALLBACKS_H_ 
#define __CMA_CALLBACKS_H_

#include <boost/function.hpp>
#include <map>
#include <string>

using std::string;
using std::map;

// namespace dbdky
// {
// namespace cma_server
// {
// class cma_frame;
// class cma_frame_legacy;									//xinsy20140305
// typedef boost::function<map<string,string> (cma_frame_legacy&)> CmaUdpFrameParserFunc;	//xinsy20140305
// typedef boost::function<string (const cma_frame_legacy&)> CmaUdpSqlInsertStringMaker;	//xinsy20140305
// typedef boost::function<map<string,string> (cma_frame&)> CmaFrameParserFunc;
// typedef boost::function<string (const cma_frame&)> CmaSqlInsertStringMaker;

// map<string,string> defaultParseFunc(cma_frame& frm);
// string defaultSqlInsertStringMaker(const cma_frame& frm);

// map<string,string> UdpdefaultParseFunc(cma_frame_legacy& frm);				//xinsy20140305
// string UdpdefaultSqlInsertStringMaker(const cma_frame_legacy& frm);			//xinsy20140305

// }
// }

#endif    //__CMA_CALLBACKS_H_
